# Question 2 — Full CRUD Notes API

## Files
- `index.js` — this is the only file you need to edit
- `spec.js` — automated tests
- `package.json` — project config and test script

## How to work on this problem
1. Open the terminal in this folder.
2. Run `npm install`.
3. Run `npm test`.
4. Complete the `FIX` markers in `index.js`.
5. Run `npm test` again until all tests pass.
